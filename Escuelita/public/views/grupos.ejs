<%- include('../partials/layout-header', { title: 'Gestión de grupos', currentPage: 'grupos' }) %>
<div class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center pt-3 pb-2 mb-4 border-bottom">
    <h1 class="h4 fw-bold">Gestión de Grupos</h1>
    <div class="d-flex align-items-center gap-3">
        <button class="btn btn-success px-4 text-white">+ Nuevo Grupo</button>
    </div>
</div>

<div class="card border-0 shadow-sm rounded-4 overflow-hidden mb-4">
    <div class="table-responsive">
        <table class="table table-striped table-hover align-middle mb-0" id="gruposTable">
            <thead class="bg-light"><tr></tr></thead>
            <tbody></tbody>
        </table>
    </div>
</div>

<script type="module">
    import { initDataTableByKey } from "../../js/features/tableLoader.js";

    $(document).ready(function() {
        initDataTableByKey('#gruposTable', 'grupos');
    });
</script>
<%- include('../partials/layout-footer') %>